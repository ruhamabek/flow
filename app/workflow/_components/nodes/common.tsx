import { TaskParamsType } from "../../../types/task";

export const ColorForHandle : Record<TaskParamsType , string> = {
      BROWSER_INSTANCE: "!bg-blue-500",    
      STRING: "!bg-red-500",  
      SELECT: "!bg-rose-300",
      CREDENTIAL: "!bg-sky-400"
}